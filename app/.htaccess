RewriteEngine On

# Zorg ervoor dat CSS correct wordt geserveerd
AddType text/css .css

# Statische bestanden uitsluiten van rewrites
RewriteCond %{REQUEST_URI} !^/assets/ [NC]

# Zorg ervoor dat assets correct worden geladen
RewriteRule ^assets/(.*)$ assets/$1 [L,NC]

# First, check if we're already on HTTPS
RewriteCond %{HTTPS} =on [OR]
RewriteCond %{HTTP:X-Forwarded-Proto} =https
RewriteRule ^ - [E=PROTO:https]

RewriteCond %{HTTP_HOST} ^www.explicittags.com [NC]
RewriteRule (.*) https://explicittags.com/$1 [L,R=301]

# Force HTTPS if not already on it and not accessing localhost
RewriteCond %{ENV:PROTO} !=https
RewriteCond %{HTTP_HOST} !^localhost [NC]
RewriteCond %{HTTP_HOST} !^127\.0\.0\.1
RewriteCond %{HTTP_HOST} ^explicittags\.com$ [NC]
RewriteCond %{REQUEST_URI} !^/\.well-known/acme-challenge/
RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# Security headers
Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains" env=HTTPS
Header always set Content-Security-Policy "upgrade-insecure-requests"
Header always set X-Frame-Options "SAMEORIGIN"
Header always set X-XSS-Protection "1; mode=block"
Header always set Referrer-Policy "strict-origin-when-cross-origin"

# Enable CORS for SSE
Header always set Access-Control-Allow-Origin "https://explicittags.com"
Header always set Access-Control-Allow-Methods "GET, POST, OPTIONS"
Header always set Access-Control-Allow-Headers "Content-Type"

# Handle EventSource CORS specifically
<FilesMatch "performers_sse\.php$">
    Header always set Cache-Control "no-cache"
    Header always set Content-Type "text/event-stream"
</FilesMatch>

# Redirect long URL to short URL - updated for both localhost and domain
RewriteCond %{HTTP_HOST} ^localhost:8000$ [OR]
RewriteCond %{HTTP_HOST} ^127\.0\.0\.1:8000$ [OR]
RewriteCond %{HTTP_HOST} ^explicittags\.com$
RewriteCond %{THE_REQUEST} \s/.*tagVideoThroughUrl/process-video\.php [NC]
RewriteRule ^utils/tagVideoThroughUrl/process-video\.php$ /utils/process-video [R=301,L]

# Handle de clean URL intern
RewriteRule ^utils/process-video$ /utils/tagVideoThroughUrl/process-video.php [L]

# Handle index path
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^index$ index.php [L]

# Remove file extensions from URLs
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME}\.(php|html|htm) -f
RewriteRule ^(.*)$ $1 [L]

# Map requests from /pages to the root directory
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^([^/]+)$ pages/$1 [L]

# Map requests without file extensions to corresponding PHP, HTML, HTM, and image files
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^([^\.]+)$ $1.php [L]

# Redirect to remove file extensions in the URL
RewriteCond %{THE_REQUEST} ^[A-Z]{3,}\s([^.]+)\.(php|html|htm) [NC]
RewriteRule ^ %1 [R=301,L]

# Prevent directory listings
Options -Indexes

# Protect against common vulnerabilities
<IfModule mod_headers.c>
    Header always set X-Download-Options "noopen"
    Header always set X-Permitted-Cross-Domain-Policies "none"
</IfModule>